from contact_tracing import Window
def get_infected_window(visit_window):
    #TODO: Need to add logic to get the window between two sanitized times
    # of the given room. 
    return Window(start = None, end = None) 